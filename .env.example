# Database
DATABASE_URL=postgresql://processflow:password@localhost:5432/processflow

# Auth
BETTER_AUTH_SECRET=generate-with-openssl-rand-base64-32
BETTER_AUTH_URL=https://processflow.merten.tech
NEXT_PUBLIC_APP_URL=https://processflow.merten.tech

# App
APP_URL=https://processflow.merten.tech

# Docker Compose (nur für Produktion)
DB_PASSWORD=your-db-password

# Custom activity URLs
# Local dev (no Docker): use localhost + port (activity-openrouter must run separately)
# Docker Compose local:  set automatically via docker-compose.local.yaml
# Production:            set automatically via docker-compose.yaml
ACTIVITY_OPENROUTER_URL=http://localhost:3031/call
